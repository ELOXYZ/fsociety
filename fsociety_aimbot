local clearTargets = {game:GetService("CoreGui"), game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui")}

for _, parent in ipairs(clearTargets) do
    if parent then
        for _, child in ipairs(parent:GetChildren()) do
            if child.Name:match("silentaim") or child.Name:match("gaming_chair") or 
               (child:IsA("ScreenGui") and child:FindFirstChild("MainFrame")) then
                child:Destroy()
            end
        end
    end
end

if getgenv().GamingChairSilentAimCleanup then
    pcall(getgenv().GamingChairSilentAimCleanup)
end

local Config = {
    Enabled = false,
    Keybind = Enum.KeyCode.V,
    FieldOfView = 350,
    Smoothness = 0.05,
    TargetPart = "Head",
    TeamCheck = true,
    WallCheck = true,
    DeathCheck = true,
    FriendCheck = true,
    FlightSpeed = 100,
    FlightEnabled = false,
    SpectateEnabled = false
}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local connections = {}
local LockedTarget = nil
local SelectedPlayer = nil
local OriginalCameraSubject = nil

getgenv().GamingChairSilentAimCleanup = function()
    Config.Enabled = false
    Config.FlightEnabled = false
    Config.SpectateEnabled = false
    if OriginalCameraSubject then
        Camera.CameraSubject = OriginalCameraSubject
    end
    for _, conn in ipairs(connections) do
        if conn and conn.Connected then conn:Disconnect() end
    end
    if getgenv().ChairFOVCircle then
        getgenv().ChairFOVCircle:Remove()
        getgenv().ChairFOVCircle = nil
    end
    local ui = ((gethui and gethui()) or game:GetService("CoreGui")):FindFirstChild("gaming_chair_silentaim_ui")
    if ui then ui:Destroy() end
    getgenv().GamingChairSilentAimCleanup = nil
end

if getgenv().ChairFOVCircle then getgenv().ChairFOVCircle:Remove() end

local FOVCircle = Drawing.new("Circle")
FOVCircle.Color = Color3.fromRGB(255, 255, 255)
FOVCircle.Thickness = 1.5
FOVCircle.Filled = false
FOVCircle.Transparency = 1
FOVCircle.Visible = true
FOVCircle.Radius = Config.FieldOfView
getgenv().ChairFOVCircle = FOVCircle

table.insert(connections, RunService.RenderStepped:Connect(function()
    if getgenv().ChairFOVCircle then
        FOVCircle.Position = UserInputService:GetMouseLocation()
        FOVCircle.Radius = Config.FieldOfView
    end
end))

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "gaming_chair_silentaim_ui"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = gethui and gethui() or game:GetService("CoreGui")

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 330, 0, 520)
MainFrame.Position = UDim2.new(0.5, -165, 0.5, -260)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 6)
local stroke = Instance.new("UIStroke", MainFrame)
stroke.Color = Color3.fromRGB(255, 255, 255)
stroke.Thickness = 1.5

local BackgroundImage = Instance.new("ImageLabel")
BackgroundImage.Size = UDim2.new(1, 0, 1, 0)
BackgroundImage.BackgroundTransparency = 1
BackgroundImage.ScaleType = Enum.ScaleType.Crop
BackgroundImage.ImageTransparency = 0.45
BackgroundImage.ZIndex = 1
BackgroundImage.Parent = MainFrame

task.spawn(function()
    pcall(function()
        local assetId = 92338628121260
        BackgroundImage.Image = "https://www.roblox.com/asset-thumbnail/image?assetId=" .. assetId .. "&width=420&height=420&format=png"
    end)
end)

Instance.new("UICorner", BackgroundImage).CornerRadius = UDim.new(0, 6)

local Header = Instance.new("TextLabel")
Header.Size = UDim2.new(1, 0, 0, 50)
Header.BackgroundTransparency = 1
Header.Text = "fsociety gaming chair upgrade"
Header.TextColor3 = Color3.fromRGB(255, 255, 255)
Header.Font = Enum.Font.Code
Header.TextSize = 23
Header.TextStrokeTransparency = 0.7
Header.ZIndex = 2
Header.Parent = MainFrame

local TabFrame = Instance.new("Frame")
TabFrame.Size = UDim2.new(1, -20, 0, 40)
TabFrame.Position = UDim2.new(0, 10, 0, 55)
TabFrame.BackgroundTransparency = 1
TabFrame.ZIndex = 2
TabFrame.Parent = MainFrame

local AimbotTab = Instance.new("TextButton")
AimbotTab.Size = UDim2.new(0.5, -5, 1, 0)
AimbotTab.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
AimbotTab.Text = "Aimbot"
AimbotTab.TextColor3 = Color3.fromRGB(0, 0, 0)
AimbotTab.Font = Enum.Font.Code
AimbotTab.TextSize = 16
AimbotTab.Parent = TabFrame
Instance.new("UICorner", AimbotTab).CornerRadius = UDim.new(0, 4)

local PlayerTab = Instance.new("TextButton")
PlayerTab.Size = UDim2.new(0.5, -5, 1, 0)
PlayerTab.Position = UDim2.new(0.5, 5, 0, 0)
PlayerTab.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
PlayerTab.Text = "Player"
PlayerTab.TextColor3 = Color3.fromRGB(255, 255, 255)
PlayerTab.Font = Enum.Font.Code
PlayerTab.TextSize = 16
PlayerTab.Parent = TabFrame
Instance.new("UICorner", PlayerTab).CornerRadius = UDim.new(0, 4)

local Content = Instance.new("Frame")
Content.Size = UDim2.new(1, -20, 1, -110)
Content.Position = UDim2.new(0, 10, 0, 105)
Content.BackgroundTransparency = 1
Content.ZIndex = 3
Content.Parent = MainFrame

local AimbotContent = Instance.new("Frame")
AimbotContent.Size = UDim2.new(1, 0, 1, 0)
AimbotContent.BackgroundTransparency = 1
AimbotContent.Visible = true
AimbotContent.Parent = Content

local PlayerContent = Instance.new("Frame")
PlayerContent.Size = UDim2.new(1, 0, 1, 0)
PlayerContent.BackgroundTransparency = 1
PlayerContent.Visible = false
PlayerContent.Parent = Content

local AimbotList = Instance.new("UIListLayout", AimbotContent)
AimbotList.SortOrder = Enum.SortOrder.LayoutOrder
AimbotList.Padding = UDim.new(0, 8)

local PlayerList = Instance.new("UIListLayout", PlayerContent)
PlayerList.SortOrder = Enum.SortOrder.LayoutOrder
PlayerList.Padding = UDim.new(0, 8)

AimbotTab.MouseButton1Click:Connect(function()
    AimbotContent.Visible = true
    PlayerContent.Visible = false
    AimbotTab.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    AimbotTab.TextColor3 = Color3.fromRGB(0, 0, 0)
    PlayerTab.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    PlayerTab.TextColor3 = Color3.fromRGB(255, 255, 255)
end)

PlayerTab.MouseButton1Click:Connect(function()
    AimbotContent.Visible = false
    PlayerContent.Visible = true
    PlayerTab.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    PlayerTab.TextColor3 = Color3.fromRGB(0, 0, 0)
    AimbotTab.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    AimbotTab.TextColor3 = Color3.fromRGB(255, 255, 255)
end)

local function createToggle(parent, name, configKey)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 35)
    frame.BackgroundTransparency = 1
    frame.Parent = parent

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = " " .. name
    label.TextColor3 = Color3.fromRGB(240, 240, 240)
    label.Font = Enum.Font.SourceSansBold
    label.TextSize = 16
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local button = Instance.new("TextButton")
    button.Size = UDim2.new(0, 55, 0, 24)
    button.Position = UDim2.new(1, -55, 0.5, -12)
    button.BackgroundColor3 = Config[configKey] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(35, 35, 35)
    button.Text = Config[configKey] and "ON" or "OFF"
    button.TextColor3 = Config[configKey] and Color3.fromRGB(0,0,0) or Color3.fromRGB(255,255,255)
    button.Font = Enum.Font.Code
    button.TextSize = 12
    button.Parent = frame

    Instance.new("UICorner", button).CornerRadius = UDim.new(0, 4)

    button.MouseButton1Click:Connect(function()
        Config[configKey] = not Config[configKey]
        button.Text = Config[configKey] and "ON" or "OFF"
        button.TextColor3 = Config[configKey] and Color3.fromRGB(0,0,0) or Color3.fromRGB(255,255,255)
        TweenService:Create(button, TweenInfo.new(0.15), {
            BackgroundColor3 = Config[configKey] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(35, 35, 35)
        }):Play()
    end)
end

local function createSlider(parent, name, min, max, configKey, decimals)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 50)
    frame.BackgroundTransparency = 1
    frame.Parent = parent

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0, 20)
    label.BackgroundTransparency = 1
    label.Text = " " .. name .. ": " .. string.format(decimals and "%.2f" or "%d", Config[configKey])
    label.TextColor3 = Color3.fromRGB(240, 240, 240)
    label.Font = Enum.Font.SourceSansBold
    label.TextSize = 16
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local bg = Instance.new("Frame")
    bg.Size = UDim2.new(1, -10, 0, 6)
    bg.Position = UDim2.new(0, 5, 0, 28)
    bg.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    bg.Parent = frame

    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((Config[configKey] - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    fill.Parent = bg

    local knob = Instance.new("ImageButton")
    knob.Size = UDim2.new(0, 12, 0, 12)
    knob.Position = UDim2.new((Config[configKey] - min) / (max - min), -6, 0.5, -6)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.Parent = bg
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local dragging = false

    local function update(input)
        local pos = input.Position.X - bg.AbsolutePosition.X
        local percent = math.clamp(pos / bg.AbsoluteSize.X, 0, 1)
        local value = min + percent * (max - min)
        if not decimals then value = math.round(value) end

        fill.Size = UDim2.new(percent, 0, 1, 0)
        knob.Position = UDim2.new(percent, -6, 0.5, -6)
        Config[configKey] = value
        label.Text = " " .. name .. ": " .. string.format(decimals and "%.2f" or "%d", value)
    end

    knob.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true end
    end)

    table.insert(connections, UserInputService.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end))

    table.insert(connections, UserInputService.InputChanged:Connect(function(inp)
        if dragging and inp.UserInputType == Enum.UserInputType.MouseMovement then
            update(inp)
        end
    end))
end

createSlider(AimbotContent, "Field of View", 10, 800, "FieldOfView")
createSlider(AimbotContent, "Smoothness", 0.05, 1, "Smoothness", true)
createToggle(AimbotContent, "Team Check", "TeamCheck")
createToggle(AimbotContent, "Wall Check", "WallCheck")
createToggle(AimbotContent, "Death Check", "DeathCheck")
createToggle(AimbotContent, "Friend Check", "FriendCheck")

createSlider(PlayerContent, "Flight Speed", 50, 300, "FlightSpeed")

local flightBtn = Instance.new("TextButton")
flightBtn.Size = UDim2.new(1, 0, 0, 35)
flightBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
flightBtn.Text = "Flight (CFrame) : OFF"
flightBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
flightBtn.Font = Enum.Font.Code
flightBtn.TextSize = 14
flightBtn.Parent = PlayerContent
Instance.new("UICorner", flightBtn).CornerRadius = UDim.new(0, 4)

flightBtn.MouseButton1Click:Connect(function()
    Config.FlightEnabled = not Config.FlightEnabled
    flightBtn.Text = "Flight (CFrame) : " .. (Config.FlightEnabled and "ON" or "OFF")
    flightBtn.BackgroundColor3 = Config.FlightEnabled and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(35, 35, 35)
    flightBtn.TextColor3 = Config.FlightEnabled and Color3.fromRGB(0,0,0) or Color3.fromRGB(255,255,255)
end)

local playerDropdown = Instance.new("TextButton")
playerDropdown.Size = UDim2.new(1, 0, 0, 35)
playerDropdown.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
playerDropdown.Text = "Select Player..."
playerDropdown.TextColor3 = Color3.fromRGB(255, 255, 255)
playerDropdown.Font = Enum.Font.Code
playerDropdown.TextSize = 14
playerDropdown.Parent = PlayerContent
Instance.new("UICorner", playerDropdown).CornerRadius = UDim.new(0, 4)

local teleportBtn = Instance.new("TextButton")
teleportBtn.Size = UDim2.new(1, 0, 0, 35)
teleportBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
teleportBtn.Text = "Teleport to Selected"
teleportBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
teleportBtn.Font = Enum.Font.Code
teleportBtn.TextSize = 14
teleportBtn.Parent = PlayerContent
Instance.new("UICorner", teleportBtn).CornerRadius = UDim.new(0, 4)

local spectateToggle = Instance.new("TextButton")
spectateToggle.Size = UDim2.new(1, 0, 0, 35)
spectateToggle.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
spectateToggle.Text = "Spectate : OFF"
spectateToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
spectateToggle.Font = Enum.Font.Code
spectateToggle.TextSize = 14
spectateToggle.Parent = PlayerContent
Instance.new("UICorner", spectateToggle).CornerRadius = UDim.new(0, 4)

local function updatePlayerList()
    playerDropdown.MouseButton1Click:Connect(function()
        local players = Players:GetPlayers()
        local currentIndex = 1
        for i, p in ipairs(players) do
            if p == SelectedPlayer then currentIndex = i break end
        end
        currentIndex = (currentIndex % #players) + 1
        SelectedPlayer = players[currentIndex]
        playerDropdown.Text = SelectedPlayer and SelectedPlayer.Name or "Select Player..."
    end)
end
updatePlayerList()

teleportBtn.MouseButton1Click:Connect(function()
    if SelectedPlayer and SelectedPlayer.Character and SelectedPlayer.Character:FindFirstChild("HumanoidRootPart") then
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = SelectedPlayer.Character.HumanoidRootPart.CFrame + Vector3.new(0, 5, 0)
        end
    end
end)

spectateToggle.MouseButton1Click:Connect(function()
    Config.SpectateEnabled = not Config.SpectateEnabled
    spectateToggle.Text = "Spectate : " .. (Config.SpectateEnabled and "ON" or "OFF")
    spectateToggle.BackgroundColor3 = Config.SpectateEnabled and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(35, 35, 35)
    spectateToggle.TextColor3 = Config.SpectateEnabled and Color3.fromRGB(0,0,0) or Color3.fromRGB(255,255,255)

    if Config.SpectateEnabled and SelectedPlayer and SelectedPlayer.Character then
        OriginalCameraSubject = Camera.CameraSubject
        Camera.CameraSubject = SelectedPlayer.Character.Humanoid
    else
        if OriginalCameraSubject then
            Camera.CameraSubject = OriginalCameraSubject
        end
    end
end)

table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Config.Keybind then
        Config.Enabled = not Config.Enabled
    elseif input.KeyCode == Enum.KeyCode.Insert then
        MainFrame.Visible = not MainFrame.Visible
    end
end))

table.insert(connections, RunService.RenderStepped:Connect(function(dt)
    if Config.FlightEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local root = LocalPlayer.Character.HumanoidRootPart
        local moveDir = Vector3.new()
        
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir += Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir -= Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir -= Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir += Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir += Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then moveDir -= Vector3.new(0,1,0) end

        if moveDir.Magnitude > 0 then
            root.CFrame = root.CFrame + (moveDir.Unit * Config.FlightSpeed * dt)
        end
    end
end))

local aimbotConn = RunService.RenderStepped:Connect(function()
    if not Config.Enabled then 
        LockedTarget = nil
        return 
    end

    local target = getClosestPlayer()
    if target and target.Character then
        local targetPart = target.Character:FindFirstChild(Config.TargetPart)
        if targetPart then
            local targetCFrame = CFrame.lookAt(Camera.CFrame.Position, targetPart.Position)
            local effectiveSmooth = 1 - Config.Smoothness
            Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, effectiveSmooth)
        end
    else
        LockedTarget = nil
    end
end)

table.insert(connections, aimbotConn)

print("fsociety loaded")
